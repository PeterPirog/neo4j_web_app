# Dangerous Scripts

Destructive Cypher belongs here and must not be executed without explicit user
approval.

Examples include broad `DETACH DELETE`, database resets, `DROP CONSTRAINT`, and
`DROP INDEX`.
